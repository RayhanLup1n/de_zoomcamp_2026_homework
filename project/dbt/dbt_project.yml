name: 'indonesia_weather_analytics'
version: '1.0.0'
config-version: 2

profile: 'indonesia_weather_analytics'

model-paths: ["models"]
macro-paths: ["macros"]
seed-paths: ["seeds"]
test-paths: ["tests"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"
